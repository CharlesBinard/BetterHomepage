# Domain configuration for Traefik
DOMAIN=yourdomain.com
ACME_EMAIL=your-email@example.com

# Node Environment
NODE_ENV=production

# Application URL (used internally by the app if needed)
NEXT_PUBLIC_APP_URL=https://yourdomain.com